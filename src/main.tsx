import React from "react";
import ReactDOM from "react-dom/client";
import LX_Player from "./LX_Player";
import "./main.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <LX_Player />
  </React.StrictMode>
);
